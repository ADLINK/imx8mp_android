From f651f7c32b4334e9104076fa634de19f97b36c39 Mon Sep 17 00:00:00 2001
From: shiva-adlink <shivashankar.t@adlinktech.com>
Date: Fri, 20 Jun 2025 15:00:33 +0530
Subject: [PATCH 2/2] sp2imx8mp: 1sec-input-snvs-pwrkey-shutdown

Signed-off-by: shiva-adlink <shivashankar.t@adlinktech.com>
---
 drivers/input/keyboard/snvs_pwrkey.c | 77 +++++++++++++++++++++++++---
 1 file changed, 71 insertions(+), 6 deletions(-)

diff --git a/drivers/input/keyboard/snvs_pwrkey.c b/drivers/input/keyboard/snvs_pwrkey.c
index 0e60b8cb0c37..e6621c529179 100644
--- a/drivers/input/keyboard/snvs_pwrkey.c
+++ b/drivers/input/keyboard/snvs_pwrkey.c
@@ -66,14 +66,16 @@ struct pwrkey_drv_data {
 	struct input_dev *input;
 	bool  emulate_press;
 	struct device *trusty_dev;
+	unsigned long press_start_jiffies;
+	bool long_press_reported;
 };
-
+/*
 static void imx_imx_snvs_check_for_events(struct timer_list *t)
 {
 	struct pwrkey_drv_data *pdata = from_timer(pdata, t, check_timer);
 	struct input_dev *input = pdata->input;
 	u32 state;
-
+pr_info("imx_snvs_check_for_events called\n");
 	if (pdata->clk) {
 		if (pdata->suspended)
 			clk_prepare_enable(pdata->clk);
@@ -95,7 +97,6 @@ static void imx_imx_snvs_check_for_events(struct timer_list *t)
 
 	state = state & SNVS_HPSR_BTN ? 1 : 0;
 
-	/* only report new event if status changed */
 	if (state ^ pdata->keystate) {
 		pdata->keystate = state;
 		input_event(input, EV_KEY, pdata->keycode, state);
@@ -103,12 +104,75 @@ static void imx_imx_snvs_check_for_events(struct timer_list *t)
 		pm_relax(pdata->input->dev.parent);
 	}
 
-	/* repeat check if pressed long */
 	if (state) {
 		mod_timer(&pdata->check_timer,
 			  jiffies + msecs_to_jiffies(REPEAT_INTERVAL));
 	}
 }
+*/
+
+static void imx_imx_snvs_check_for_events(struct timer_list *t)
+{
+        struct pwrkey_drv_data *pdata = from_timer(pdata, t, check_timer);
+        struct input_dev *input = pdata->input;
+        u32 state;
+        unsigned long now = jiffies;
+
+        pr_info("imx_snvs_check_for_events called\n");
+
+        if (pdata->clk) {
+                if (pdata->suspended)
+                        clk_prepare_enable(pdata->clk);
+                else
+                        clk_enable(pdata->clk);
+        }
+
+        if (pdata->trusty_dev)
+                state = trusty_snvs_read(pdata->trusty_dev, SNVS_HPSR_REG);
+        else
+                regmap_read(pdata->snvs, SNVS_HPSR_REG, &state);
+
+        if (pdata->clk) {
+                if (pdata->suspended)
+                        clk_disable_unprepare(pdata->clk);
+                else
+                        clk_disable(pdata->clk);
+        }
+
+        state = state & SNVS_HPSR_BTN ? 1 : 0;
+
+        /* only report new event if status changed */
+        if (state ^ pdata->keystate) {
+                pdata->keystate = state;
+                input_event(input, EV_KEY, pdata->keycode, state);
+                input_sync(input);
+                pm_relax(pdata->input->dev.parent);
+
+                if (state) {
+                        // Button just pressed, start timing
+                        pdata->press_start_jiffies = now;
+                        pdata->long_press_reported = false;
+                } else {
+                        // Button released, reset flag
+                        pdata->long_press_reported = false;
+                }
+        }
+
+        /* check for 1 second press */
+        if (state) {
+                if (!pdata->long_press_reported &&
+                    time_after_eq(now, pdata->press_start_jiffies + msecs_to_jiffies(1000))) {
+                        pr_info("Power key pressed for 1 second!!!!!!\n");
+                        pdata->long_press_reported = true;
+			regmap_update_bits(pdata->snvs, SNVS_LPCR_REG, BIT(5), BIT(5));//dumb set
+			regmap_update_bits(pdata->snvs, SNVS_LPCR_REG, BIT(6), BIT(6));//power off reg
+                }
+
+                /* repeat check */
+                mod_timer(&pdata->check_timer,
+                          now + msecs_to_jiffies(REPEAT_INTERVAL));
+        }
+}
 
 static irqreturn_t imx_snvs_pwrkey_interrupt(int irq, void *dev_id)
 {
@@ -277,9 +341,10 @@ static int imx_snvs_pwrkey_probe(struct platform_device *pdev)
 
 	if (pdata->trusty_dev != NULL)
 		trusty_snvs_update_lpcr(pdata->trusty_dev, SNVS_LPCR_DEP_EN, 1);
-	else
+	else{
 		regmap_update_bits(pdata->snvs, SNVS_LPCR_REG, SNVS_LPCR_DEP_EN, SNVS_LPCR_DEP_EN);
-
+		regmap_update_bits(pdata->snvs, SNVS_LPCR_REG, BIT(16), BIT(16));//settin for 10 sec...no 1 sec??
+	}
 	/* clear the unexpected interrupt before driver ready */
 	if (pdata->trusty_dev != NULL)
 		trusty_snvs_write(pdata->trusty_dev, SNVS_LPSR_REG, SNVS_LPSR_SPO);
-- 
2.34.1

